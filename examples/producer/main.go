package main

func main() {
	// Producer(glob.Config.Topic, msgCount)
}

// func Producer(topic string, count int) {
// 	for i := 1; i <= count; i++ {
// 		msg := fmt.Sprintf("%v.-%v (%v)", i, time.Now().Format("01/02 15:04:05"), topic)
// 		dispatcher.Send(topic, []byte("go-dis"), []byte(msg), errorHandler)
// 	}
// }
